# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/tabflow
# For MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tabflow?retryWrites=true&w=majority

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000
# For production:
# NEXT_PUBLIC_API_URL=https://tabflow-api.vercel.app

# Extension ID (after publishing to Chrome Web Store)
# CHROME_EXTENSION_ID=your-extension-id-here